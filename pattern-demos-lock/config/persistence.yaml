# yaml-language-server: $schema=https://json-schema.axonivy.com/14.0-dev/config/persistence-2.json
Persistence:
  pattern-demos-lock:
    DataSource: pattern-demos-lock
    Managed:
      Classes:
        - com.axonivy.demo.patterndemos.lock.entities.Lock
    Properties:
      hibernate.hbm2ddl.auto: create-drop
